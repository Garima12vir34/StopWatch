# StopWatch
For a frontend project, select Node from the .gitignore template dropdown — it's the most common choice and will cover any accidental editor/OS files. But honestly for pure HTML/CSS/JS, No .gitignore is perfectly fine too.
